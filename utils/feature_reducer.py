class FeatureReducer:

    def reduceFeatures(self, TotTermFreq_Dictionary):
        for each_feature in range(len(TotTermFreq_Dictionary.keys())):
	    if TotTermFreq_Dictionary[each_feature] = 1:
	        del TotTermFreq_Dictionary[each_feature]
	return TotTermFreq_Dictionary
